libfoo is a tiny extra library used in this project.

Since it's a separate library, it has its own rules.mk. You need to
include this rules.mk in your USER_MODULES when building your
project. See make-helper.sh.

